# Ignore the `node_modules` directory, which contains all the dependencies installed via npm.
# These files are large and can be recreated at any time by running `npm install`.
node_modules/

# Ignore the `data` directory, which might contain files generated by the FTP server,
# These files are often specific to the development or production environment.
data/

# Ignore the `.env` file, which contains environment variables.
# This file should not be shared for security reasons.
.env

# Ignore the `startup.bat` file, a Windows-specific batch script used to start PM2.
# This file is often specific to your local environment and doesn't need to be
# shared with other project members.
startup.bat
